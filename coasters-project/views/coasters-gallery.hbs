<div class="container">
    <div class="row">
        <h1>Gallery</h1>
        {{#each coasters}}
        <div class="col-lg-4">
            {{>coasterpartial}}
        </div>
        {{/each}}
    </div>
</div>