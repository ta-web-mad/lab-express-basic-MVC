<div class="container">
    <h1>Craziest Coasters</h1>
    <div class="row">
        {{#each coasters}}
        <div class="col-md-4 mb-4">
            <div class="card">
                <img src="{{imageUrl}}" class="card-img-top" alt="{{title}}">
                <div class="card-body">
                    <h5 class="card-title">{{title}}</h5>
                    <h6>Length: {{length}}</h6>
                    <h6>Inversions: {{inversions}}</h6>
                    <p>{{description}}</p>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    <hr>
    <a href="/" class="btn btn-dark mb-5">Home</a>
</div>