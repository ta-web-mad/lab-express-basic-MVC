<div class="container mt-5">
    <h1>Montañas Rusas mas largas</h1>
    <hr>

    <div class="row">
        {{#each crazyCoasters}}
        <div class="col-4 mb-5">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top coasterImg" src="{{imageUrl}}" alt="{{name}}">
                <div class="card-body">
                    <h4>{{title}}</h4>
                    <p class="card-text">{{description}}</p>
                    <h6>Inversions: {{inversions}}</h6>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

</div>